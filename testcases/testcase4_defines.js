
define(['N/file','N/record'], function(file,record){
	return {
		//function to generate any data and return the ids 
	    generateData : function(){

		    return [];//this can be a json also
	    },
	    //The obj will be the set of ids returned by generateData
	    assertData : function(obj){
	    	if(c1==)
		    return true;//This function must return a true or false
			if(c2 ==)
	    },
	  //The obj will be the set of ids returned by generateData.Once asserted the data can be deleted here
	    deleteData : function(obj){
	    }
	};
});